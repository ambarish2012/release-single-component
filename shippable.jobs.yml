jobs:
  - name: demo-env-variable-manifest-job
    type: manifest
    steps:
      - IN: demo-img
      - IN: demo-img-opts
      - TASK: managed
    flags:
      - release-single-component

  - name: demo-env-variable-deploy-job
    type: deploy
    steps:
      - IN: demo-env-variable-manifest-job
      - IN: demo-env-variable-ecs-cluster
      - TASK: managed
    flags:
      - release-single-component

  - name: demo-env-variable-runSh-job                                
    type: runSh                                 
    steps:                                      
      - IN: deploy-test-ecs                 
      - TASK:
        - script: echo "hello"
        
